import "./InProgress.scss";

const InProgress = () => {
    return (
        <div className="container__in-progress">
           <h1>In Progress...</h1>
        </div>
    );
};

export default InProgress;